# Copy to .env.local
# API base URL override (opsional). Tanpa ini, URL ditentukan saat build dari
# branch (lihat vite.config.ts: main → https://api.qouver.com/majadu,
# dev → https://api.qouver.com/majadu-dev). Untuk dev lokal ke Go backend:
VITE_API_URL=http://localhost:8080
